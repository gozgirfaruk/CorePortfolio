@using Portfolio.Entity.Concrete
@model List<WriterMessage>
@{
    ViewData["Title"] = "SenderMessage";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}
<html lang="en">

<body>

    <div class="container-fluid page-body-wrapper">

        <div class="content-wrapper">

            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">GİDEN KUTUSU</h4>
                        <p class="card-description">
                            Tablo:  <code>.Duyurular</code>
                        </p>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Konu</th>
                                        <th>Alıcı</th>
                                        <th>Tarih</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <th>@item.WriterMessageID</th>
                                            <td>@item.Subject</td>
                                            <td>@item.ReceiverNAme</td>
                                            <td>@item.Date.ToString("dd.MM.yyyy")</td>
                                            <td><a href="/Writer/Message/SenderMessageDetail/@item.WriterMessageID" class="btn btn-outline-secondary">Mesajı Gör</a></td>
                                        </tr>
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <!-- page-body-wrapper ends -->
    </div>

</body>

</html>

